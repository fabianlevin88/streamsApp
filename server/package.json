{
  "name": "server",
  "version": "1.0.0",
  "description": "Streams App server",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 -w db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fabian Andres Levin",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
